{% extends '@App/base/base.html.twig' %}



{% block content %}
<section>
    <article>
        <div class="grandeimage">
            <img src="{{ asset('img/image-entete.jpg') }}" alt="deux escrimeurs" class="image" width="100%">
        </div>
    </article>


    <article>
        <div class="container my-5 texte">
        <h1>Actualités du club</h1>
        </div>
        <div>
            {% for actu in actus  %}
                <div class="container my-5 pb-3 texte">
                    <div class="container">
                        <h3 class="my-3">{{ actu.titre }}</h3>
                            {% if actu.photo %}
                                <div class="imageactus"><img src="{{ asset('uploads/images/' ~ actu.photo) }}" alt="image de l'actualite"  width="100%"></div>
                            {% else %}
                                <p>pas d'image</p>
                            {% endif %}
                        <p>{{ actu.contenu }}</p>
                    </div>
                </div>

            {% endfor %}
        </div>
    </article>
</section>
{% endblock %}